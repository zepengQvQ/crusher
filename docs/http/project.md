# docs/http/

这里放「点一下就能发请求」的脚本，方便你在编辑器里试接口。

1. 装好 VS Code 的 REST Client 插件（或用 IDEA 自带的 HTTP Client）
2. 打开 `示例-分析接口.http`
3. 确认文件里的地址是本机后端（默认 `http://127.0.0.1:8000`）
4. 点请求旁边的 Send / 发送

作用：不用打开网页，也能直接试「提交分析」「查任务」接口。
