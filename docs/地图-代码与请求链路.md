# 开发地图（给 Java 同学）

启动步骤见根目录 `README.md`。  
本文只回答两件事：**一次分析怎么走完**、**想改功能打开哪个文件**。

## 1. 用户点一次「分析」，背后发生了什么

```text
手机页：粘贴条款，点分析
  → 后端收到「新建分析」请求
  → 立刻返回一个任务编号（task_id），页面开始转圈
  → 后台慢慢做：
       检查输入 → 猜产品类型 → 抽关键数字/条款
       → 用规则再核对一遍 → 检查证据是否真在原文里
       → 写通俗解释 → 发布门禁（publish / 部分 / 澄清 / 拒答）
  → 结果暂存在「内存小本本」里（重启后端就没了）
  → 页面反复问「任务好了没」
  → 好了进报告页；失败进错误页
```

补充：

- 默认 `MOCK_MODE=true`：不真的去调大模型（省 Key、可断网演示）；规则核对仍是真的。
- 页面上「模拟模型超时」等按钮：故意让任务失败，用来演示错误页。
- 正式演示路径**不会**把可选 MCP 挂进 H5 主链路；也不画模型生成的流程图。
- 同属 Demo 的其它入口：上传抽正文、双材料对照、追问、试算、对比、纠错重分析（见下表）。

## 2. 想改哪里，打开哪个文件

| 你想改的事 | 打开这个文件 | 可以当成 Java 里的… |
|------------|--------------|---------------------|
| 接口 URL、入参出参 | `backend-python/app/interfaces/http/routes.py` | Controller |
| 「提交任务、后台跑分析」整段流程 | `backend-python/app/application/analyze_text.py` | Application Service |
| 双材料对照 | `backend-python/app/application/analyze_dual_sources.py` | Application Service |
| 文档抽正文 | `backend-python/app/application/extract_document.py` | Application Service |
| 追问 / 试算 / 对比 | `answer_from_evidence.py` / `calculate_scenario.py` / `compare_products.py`（同目录） | Application Service |
| 意图 / 完整性 | `resolve_intent.py` / `check_input_completeness.py` | Application Service |
| 意图分发（六种 UseCase） | `analysis_dispatcher.py` | Application Service / Facade |
| 纠错重分析 | `backend-python/app/application/reanalyze_with_correction.py` | Application Service |
| 分析流水线与发布门禁 | `backend-python/app/application/analysis_harness.py` | Application Service |
| 把各模块 new 出来、接在一起 | `backend-python/app/composition_root.py` | `@Configuration` |
| 「任务库 / 知识库 / 大模型」该长什么样（接口） | `backend-python/app/domain/ports/protocols.py` | `interface` |
| 关键词、否定句、数值规则 | `backend-python/app/domain/rules/engine.py` | 领域服务 |
| 从原文抽参数 | `backend-python/app/domain/rules/fact_extractor.py` | 领域服务 |
| 证据必须能在原文找到 | `backend-python/app/domain/rules/evidence.py` | 领域服务 |
| 读 `knowledge/*.json` | `backend-python/app/infrastructure/knowledge/local_files.py` | Repository 实现 |
| 假大模型 / 真大模型调用 | `backend-python/app/infrastructure/llm/mock_gateway.py` | Gateway 实现 |
| 读 `.env` 配置 | `backend-python/app/config/settings.py` | ConfigurationProperties |
| 手机页跳转 | `frontend-h5/src/router/index.js` | 前端 Router |
| 可选本地 MCP | `backend-python/app/interfaces/mcp/` | 旁路适配（不进 HTTP 主链路） |

## 3. Python 在本项目里怎么理解（别背概念）

| 你看到的 | 白话 |
|----------|------|
| `pyproject.toml` + `pip install -e .` | 类似 Maven 的「装这个项目当依赖」 |
| `requirements.lock` | 依赖版本锁死，大家装一样的 |
| `.env` | 本地配置文件（Mock 开关、密钥等） |
| `composition_root.py` | 手工 `new` 对象并传给别人用（构造函数注入），没有奇奇怪怪的注解 |
| `Protocol` | 就是 Java 的 `interface` 那种「约定方法签名」 |
| `async` / 后台任务 | 接口先秒回任务号；真正分析在后台跑。规则核对本身是普通同步代码 |

## 4. 容易懵的词（小抄）

| 词 | 意思 |
|----|------|
| Mock | 假的：假装调了大模型，方便演示 |
| task_id | 这次分析的编号 |
| publication | 发布决策：能否展示报告 / 要澄清 / 拒答 |
| revision | 用户纠错后相对父任务的一次修订 |
| 金标 / golden | 「标准答案」样例，测试要对着它 |
| 回归 | 改完代码再跑一遍，防老问题又冒出来 |
| OpenAPI | 接口说明书（机器可读） |
| 契约 | 「前后端约好字段不能乱改」 |
| 端口 / ports | 这里不是网络端口，是「接口定义」那个意思 |
| 适配器 / infrastructure | 真正连文件、连大模型的实现代码 |
