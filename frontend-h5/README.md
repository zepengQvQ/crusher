# crusher-frontend-h5

Vue3 + Vant 手机网页（Demo 正式前端）。

```bash
cd frontend-h5
npm install
npm run dev
```

默认 http://localhost:5173 ，Vite 代理后端 `/api` 与 `/health`。

更完整的启动步骤见仓库根目录 `README.md` / `make setup`。
