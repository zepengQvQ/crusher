"""
提示词模板模块
集中管理三阶段提示词，便于调优和维护。

注意：STAGE1 / STAGE3 使用 `.replace("{placeholder}", ...)` 注入变量，
而非常规 `.format()` —— 因为注入的知识库事实文本本身包含 JSON 花括号，
`.format()` 会把它们误当作格式化占位符。
"""

# ============================================================
# 阶段一：白话翻译 + 结构化提取
# ============================================================

SYSTEM_PROMPT = """你是一位金融条款的"翻译官"和"组织者"。
你的任务是把晦涩难懂的金融条款转成普通人能听懂的大白话，并提取关键要素、识别风险点。

【重要】你不是金融知识的来源。你的金融知识只能来自下方用户消息中提供的【知识库事实】，
禁止使用你自己的金融知识或训练记忆中的任何金融定义、数值、规则。

硬性规则：
1. 只能引用【知识库事实】中出现的定义、风险等级、监管规则、数值。
2. 【知识库事实】中查不到的术语/概念，一律标注"未收录"，绝不编造定义。
3. 原文未明确写出的具体数值，一律写"原文未说明"，绝不从常识补数字。
4. product_type 只能从【知识库事实】的「识别到的产品类型」里选；若未识别到，写"未收录"。
5. risk_level 只能在【知识库事实】的产品风险等级提示范围内取值，除非原文明确写明了等级。
6. 引用监管规则时，只能引用【知识库事实】给出的监管要点，不得补充其他法规条文。
7. 说话通俗、贴切，善用生活中的比喻，但不得为了幽默而歪曲事实。
"""

STAGE1_USER_PROMPT = """【知识库事实】
{knowledge_context}

请分析以下这段金融条款文本：

---
{raw_text}
---

请严格按以下 JSON 格式输出（不要输出 JSON 以外的任何内容）：

{
  "plain_language": "用100字以内的大白话，基于【知识库事实】说清楚这到底是个什么产品、怎么赚钱、怎么亏钱，语言通俗幽默",
  "product_type": "产品类型，只能从【知识库事实】的「识别到的产品类型」里选",
  "term": "投资期限，如90天/1年/无固定期限",
  "expected_return": "预期收益率描述，如1.5%~5.2%/不保证收益",
  "risk_level": "风险等级，如R1/R2/R3/高风险/中风险，按【知识库事实】的风险等级提示",
  "early_redemption": "提前赎回条件，如不可赎回/需支付违约金/随时可赎",
  "fee_structure": "费用结构简述，如管理费0.5%/无额外费用",
  "principal_protection": "本金保障情况，如保本/不保本/部分保本，按【知识库事实】的本金保障情况",
  "key_logic": "用2-3句话描述收益触发的核心逻辑（用于后续生成流程图）；结构化产品务必区分：定期观察日校验、任意交易日触发、到期结算，不要把多类条件揉成一句笼统描述"
}

注意：
- 所有金融知识只能来自上方【知识库事实】，查不到的字段填"未收录"
- 如果原文未提及某项具体数值，填"原文未说明"
- plain_language 必须控制在100字以内
- 所有值都用中文
"""


# ============================================================
# 阶段二：Mermaid 流程图生成
# ============================================================

STAGE2_USER_PROMPT = """根据以下金融产品的核心逻辑，生成一段 Mermaid flowchart TD 代码，描述资金流向和收益触发逻辑。

产品逻辑：
{logic_summary}

要求：
1. 节点文字用中文，简洁明了，每个节点标签不超过15个字
2. 箭头方向清晰，从"买入产品"开始
3. 赚钱/高收益的路径节点用绿色，添加样式：style 节点ID fill:#e1f5e1,stroke:#2e7d32,color:#1b5e20
4. 亏钱/低收益/本金受损的路径节点用红色，添加样式：style 节点ID fill:#ffe1e1,stroke:#c62828,color:#b71c1c
5. 中性节点用默认样式
6. 【重要】节点标签和边标签中绝对不要使用以下字符，会导致Mermaid解析失败：
   - 问号 ? → 用"是否""能否"等陈述式表述
   - 小于号 < → 用"低于""小于"或全角 ＜
   - 大于号 > → 用"高于""大于"或全角 ＞
   - 反斜杠换行 \n → 节点内不要换行，保持单行
7. 节点ID只用英文字母（如A、B、C、D），不要用中文或数字作为ID
8. 只输出 Mermaid 代码块（用 ```mermaid 包裹），不要输出其他解释文字

示例结构：
```mermaid
flowchart TD
    A["买入产品"] --> B{"观察日条件是否满足"}
    B -->|是| C["获得高收益"]
    B -->|否| D["仅获低收益"]
    style C fill:#e1f5e1,stroke:#2e7d32,color:#1b5e20
    style D fill:#ffe1e1,stroke:#c62828,color:#b71c1c
```
"""


# ============================================================
# 阶段三：风险识别与高亮
# ============================================================

STAGE3_USER_PROMPT = """【知识库风险事实】（优先检查清单，命中这些模式的原文片段应被识别为风险点）
{matched_risk_context}

请仔细阅读以下金融条款，找出所有可能对消费者不利的表述、容易被忽略的风险点、或文字游戏。

原文：
---
{raw_text}
---

请严格按以下 JSON 数组格式输出（不要输出 JSON 以外的任何内容）：

[
  {
    "snippet": "原文中对应的片段（必须是原文中连续出现的文字，用于前端高亮匹配）",
    "risk_level": "高/中/低",
    "explanation": "一句话解释为什么这对消费者不利或容易误解，通俗易懂"
  }
]

要求：
1. 优先对照上方【知识库风险事实】中的风险模式，在原文中定位对应片段，explanation 应基于该事实给出
2. snippet 必须是原文中精确存在的连续文字片段，不要改写
3. 【知识库风险事实】没有覆盖的风险，可以补充识别，但 explanation 只能基于原文文字本身，不得引入外部金融知识
4. 只输出 0～N 个有原文证据的风险点，最多 8 个；禁止为了凑数量制造风险
5. 按风险等级从高到低排序
6. 如果文本非常安全无明显风险，可以返回空数组 []
"""


# ============================================================
# 语气风格切换
# ============================================================

STYLE_PRESETS = {
    "通俗版": "用最通俗的大白话，像跟邻居聊天一样解释",
    "大妈版": "用菜市场大妈都能听懂的话，带点幽默和接地气的比喻",
    "专业版": "用严谨的金融术语解释，但补充通俗注释",
    "幽默版": "用轻松幽默的语气，适当使用网络流行语，但保证信息准确",
}
