# tests/

正式 Demo 的自动测试（`make test` 跑这里；旧版测试在 `legacy/tests/`，不跑）。

| 子目录 | 白话 |
|--------|------|
| `fixtures/` | 测试用的输入和「标准答案」 |
| `api/` | 直接打接口看对不对 |
| `p0_01` … `p0_09` | 按整改编号拆开的验收用例 |
| `p0_rc_01` … `p0_rc_02` | P0 收口修复项（真模型网关、规则边界等） |

根目录还有对照用 JSON：`对照-期望输出样例.json`、`样例-结构性存款.json`。
