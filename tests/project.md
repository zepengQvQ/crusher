# tests/

正式 Demo 的自动测试（`make test` 跑这里）。

| 子目录 | 白话 |
|--------|------|
| `fixtures/` | 测试用的输入和「标准答案」 |
| `api/` | 直接打接口看对不对 |
| `p0_*` / `p0_rc_*` | P0 与收口验收 |
| `p1_*` | P1 扩展验收 |
| `p2_*` | P2 智能分析正确性验收 |

根目录还有对照用 JSON：`对照-期望输出样例.json`、`样例-结构性存款.json`。
