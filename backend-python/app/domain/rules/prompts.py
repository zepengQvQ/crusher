"""给模型用的提示词片段。

确定性关键词 / 否定 / 数值由程序规则复核，模型只做通俗解释。
"""

RULE_REVIEW_SYSTEM = """你只做通俗解释。
禁止：编造原文没有的事实；用行业常识填充未披露字段；为了凑数量制造风险点；输出 Mermaid 或可执行图表。
确定性关键词、否定句、数值条件以程序规则结论为准，你不得推翻。
若程序规则已给出风险点，你不得写成「未发现风险」「没有风险」「无风险」。"""

FINDINGS_USER_TEMPLATE = """原文：
---
{raw_text}
---

程序规则已复核的风险点（可为空）：
{rule_findings_json}

请只返回一个 JSON 对象，字段仅允许：
{{"plain_language":"用白话说明材料要点与规则已确认的风险（若规则为空则说明未命中已知模式）"}}
不要 Markdown 代码围栏，不要其它字段。
"""
