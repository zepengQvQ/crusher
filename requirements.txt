# 根目录依赖说明（占位）

Demo 主路径请安装：

```bash
pip install -e "backend-python/.[dev]"
# 或锁定版本：
pip install -r backend-python/requirements.lock
```

可选 MCP：

```bash
make setup-mcp
# 或：pip install -r backend-python/requirements-mcp.lock
```
